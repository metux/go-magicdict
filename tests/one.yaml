foo:
    - one
    - two
    - three

bar:
    x: x-one
    y: y-one
    tree:
        myname: ${@@KEY}
        leaf: 1.2
        leaf2:
        list:
            - a
            - b
xxx: huhu

# abc: will be created by test

hello: ${xxx}

ref: ${bar}

butter:
    - b1
    - [ "abc1", "abc2" ]
    - b3

zzz: ${@@KEY}xx

x123:
    one:
        two: name-is-${@@KEY}
        three: parent-is-${@@PARENT::@@KEY}

schinken: ${brot::wurst}

target1:
    name: one
    foo: bar
