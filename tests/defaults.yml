foo:
    - foo1
    - foo2
      - a
      - b

bar:
    x: x-default
    tree:
        leaf: 1.2
        leaf2:
        knollo: foobar-${foo::0}
        knollo2: foobar-${foo::0}-for-me

yyy: huhu

escapetest: foo-\${foo}

tmpl1:
    c/executable:
        install/dir: /usr/bin
        install/mode: 0766

target1: ${tmpl1::c/executable}
